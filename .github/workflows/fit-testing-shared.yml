name: Run FIT Testing (Internal/Shared)

on:
  workflow_dispatch:
    inputs:
      preset:
        description: FIT preset(s) and/or preset group(s) to generate and execute, comma-separated. (`fit definition list-presets`)
        required: true
        type: string
      collect_extra_diagnostics:
        description: Collect additional diagnostics (includes cbcollect, is slower)
        default: true
        type: boolean
      performer:
        description: Performer image name to use e.g. "java-fit-performer:main".
        required: true
        type: string
      channel:
        description: fit-cli install channel. ("latest" or "ci")
        required: false
        type: string
        default: ci
      fit_cli_ref:
        description: >
          Optional couchbaselabs/fit-cli git ref (branch, tag, or `refs/pull/<n>/head`)
          to build fit-cli from source instead of installing `channel`. For testing a
          fit-cli branch or PR.
        required: false
        type: string
        default: ''
  workflow_call:
    inputs:
      preset:
        description: FIT preset(s) and/or preset group(s) to generate and execute, comma-separated. (`fit definition list-presets`)
        required: true
        type: string
      collect_extra_diagnostics:
        description: Collect additional diagnostics (includes cbcollect, is slower)
        required: false
        type: boolean
        default: true
      performer:
        description: Performer image name to use e.g. "java-fit-performer:main".
        required: true
        type: string
      channel:
        description: fit-cli install channel. ("latest" or "ci")
        required: false
        type: string
        default: ci
      fit_cli_ref:
        description: >
          Optional couchbaselabs/fit-cli git ref (branch, tag, or `refs/pull/<n>/head`)
          to build fit-cli from source instead of installing `channel`. For testing a
          fit-cli branch or PR.
        required: false
        type: string
        default: ''

permissions:
  id-token: write   # required for GitHub OIDC

jobs:
  preset-matrix:
    runs-on: ubuntu-latest
    outputs:
      presets: ${{ steps.presets.outputs.presets }}
    steps:
      - uses: actions/checkout@v4

      # Expands any preset group names (e.g. "qe-release") in $PRESETS into their
      # member presets, so the matrix below only ever sees concrete preset names.
      - name: Install fit-cli
        uses: ./.github/actions/install-fit-cli
        with:
          channel: ${{ inputs.channel }}
          ref: ${{ inputs.fit_cli_ref }}

      - name: Preset matrix
        id: presets
        shell: bash
        run: |
          presets=$(fit definition expand-preset-group "$PRESETS" --json)
          echo "presets=$presets" >> "$GITHUB_OUTPUT"
        env:
          PRESETS: ${{ inputs.preset }}

  run-fit:
    needs: preset-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        preset: ${{ fromJson(needs.preset-matrix.outputs.presets) }}
    name: Run FIT (${{ matrix.preset }})
    steps:
      - uses: actions/checkout@v4

      - name: Install fit-cli
        uses: ./.github/actions/install-fit-cli
        with:
          channel: ${{ inputs.channel }}
          ref: ${{ inputs.fit_cli_ref }}

      # Assume the AWS role 'fit-cli-role' from account 'cb-sdk' (958525475024), as this has all required AWS permissions.
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-region: us-west-2
          role-to-assume: arn:aws:iam::958525475024:role/fit-cli-role
          role-session-name: gha-${{ github.actor }}-${{ github.run_id }}
          audience: sts.amazonaws.com
          retry-max-attempts: 3
          role-duration-seconds: 43200

      - name: Run fit-cli
        run: |
          args=(preset "${{ matrix.preset }}" --performer "${{ inputs.performer }}")
          if [[ "${{ inputs.collect_extra_diagnostics }}" == "true" ]]; then
            args+=(--cbcollect)
          fi

          fit run "${args[@]}"
